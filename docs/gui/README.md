# GUI

Running locally (preventing CORS):

```
# MUST start from top-level dir of search-query!
python -m http.server 8000
```

Note: when developing locally, the temporary (cached) files may need to be removed regularly.
